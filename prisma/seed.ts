import {PrismaClient} from "@prisma/client";import {randomBytes,scryptSync} from "crypto";const db=new PrismaClient();function hashPassword(password:string){const salt=randomBytes(16).toString("hex");return `${salt}:${scryptSync(password,salt,64).toString("hex")}`;}async function main(){const adminPassword=process.env.ADMIN_PASSWORD||"Admin@123";await db.user.upsert({where:{email:"admin@attoflow.local"},update:{},create:{name:"Administrador AttoFlow",email:"admin@attoflow.local",passwordHash:hashPassword(adminPassword),nickname:"ATTO ADM",role:"ADMIN",credits:9999,referralCode:"ATTOADMIN"}});if(!await db.tournament.count())await db.tournament.createMany({data:[{title:"AttoFlow Rush #01",slug:"attoflow-rush-01",description:"Sala competitiva rápida para squads.",mode:"Squad",map:"Bermuda",maxPlayers:48,entryCredits:20,prizeCredits:500,startsAt:new Date(Date.now()+86400000),status:"OPEN",rules:"Fair play obrigatório."},{title:"Solo Insano",slug:"solo-insano",description:"Disputa individual para quem confia na trocação.",mode:"Solo",map:"NexTerra",maxPlayers:36,entryCredits:10,prizeCredits:250,startsAt:new Date(Date.now()+172800000),status:"OPEN",rules:"Fair play obrigatório."}]});}main().finally(()=>db.$disconnect());
