import {db} from "@/lib/db";import {getCurrentUser} from "@/lib/auth";import {joinTournamentAction} from "@/app/actions/tournaments";export default async function Tournaments({searchParams}:{searchParams:Promise<{error?:string,success?:string}>}){const q=await searchParams;const user=await getCurrentUser();const camps=await db.tournament.findMany({where:{status:{in:["OPEN","LIVE"]}},orderBy:{startsAt:"asc"},include:{_count:{select:{registrations:true}}}});return <main className="section"><span className="eyebrow">CAMPEONATOS</span><h1>Escolha seu próximo camp.</h1><p className="lead">Competições com taxa de entrada em créditos e premiação definida pela organização. Sem odds ou aposta em resultado.</p>{q.error&&<p className="alert">{q.error}</p>}{q.success&&<p className="success">{q.success}</p>}<div className="campgrid">{camps.map(c=><article className="camp" key={c.id}><div className="camptop"><span>{c.status}</span><b>{c.mode}</b></div><h2>{c.title}</h2><p>{c.description}</p><div className="campmeta"><span>Mapa <b>{c.map}</b></span><span>Vagas <b>{c._count.registrations}/{c.maxPlayers}</b></span><span>Entrada <b>{c.entryCredits} cr</b></span><span>Prêmio <b>{c.prizeCredits} cr</b></span></div><small>{new Date(c.startsAt).toLocaleString("pt-BR")}</small>{user?<form action={joinTournamentAction}><input type="hidden" name="tournamentId" value={c.id}/><button className="btn" disabled={c.status!=="OPEN"}>Inscrever-se</button></form>:<a className="btn" href="/login">Entrar para jogar</a>}</article>)}</div></main>}
